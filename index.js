/**
 * @format
 */

import { AppRegistry } from 'react-native';
import BiggyCore from './BiggyCore';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => BiggyCore);
